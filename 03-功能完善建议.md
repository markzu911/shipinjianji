# AI 口播视频剪辑 — 功能完善建议

> 本文档聚焦**现有功能的打磨与补充**，不涉及大功能模块的新增。每条建议标注了优先级和改动范围。

---

## 一、核心编辑体验

### 1.1 撤销 / 重做（Undo / Redo）

**现状：** 用户在"文字剪辑"中误删文字后，只能刷新页面重新开始，没有任何回退机制。

**建议：** 为删除操作增加撤销栈（至少支持 20 步），快捷键 Ctrl+Z / Ctrl+Y。技术上只需在前端维护一个操作历史数组（快照或命令模式），服务端不需要改动。

- **优先级：** 🔴 高
- **改动范围：** 前端 `app.js`

---

### 1.2 手动修正 ASR 转录文字

**现状：** 语音识别结果如果有错（如人名、专业术语），用户无法手动修改，只能接受错误或整段删除。但 Paraformer 实时模型本身对专有名词的识别率有限，这在口播视频中是常见痛点。

**建议：** 在"识别全文"标签页中，让每个词段（word chip）的文字可双击编辑。修改后自动更新对应词段的时间戳和文本，同时同步到剪切时间轴的显示。不需要重新调用 ASR。

- **优先级：** 🔴 高
- **改动范围：** 前端 `app.js` + 后端 `app.py`（更新内存中的 segment 数据）

---

### 1.3 删除范围的精确时间输入

**现状：** 时间轴上的删除范围只能通过拖动两端手柄来调整，拖动精度受限于屏幕像素。对于需要精确到帧的裁剪（如去掉某个词的前半截），操作非常困难。

**建议：** 在删除范围被选中时，显示开始时间和结束时间的数字输入框（格式 `MM:SS.ms` 或帧号），允许键盘直接输入精确值。这些输入框可与拖动手柄双向联动。

- **优先级：** 🟡 中
- **改动范围：** 前端 `app.js`（时间轴交互部分）

---

### 1.4 提交剪切前的本地预览

**现状：** 用户标记完删除范围后点击"生成剪切"，必须等待后台 FFmpeg 完整渲染才能看到效果。如果对结果不满意（比如删多了），需要重新调整再渲染，耗时较长。

**建议：** 利用浏览器的 `HTMLVideoElement` 和 `MediaSource`/时间区间跳过的能力，在前端模拟"跳过已删除区间"的播放效果。用户可以在提交前先预览大致的删除节奏，确认后再触发服务端渲染。不需要瞬时的预览视频，只需要播放时自动跳过被标记的区间即可。

- **优先级：** 🟡 中
- **改动范围：** 前端 `app.js`（时间轴 + 播放器联动逻辑）

---

### 1.5 一键应用全部 AI 建议

**现状：** AI 建议面板列出了所有检测到的口误、重复、填充词等，但用户需要逐条点击"标记删除"。对于有 20+ 条建议的视频，操作很繁琐。

**建议：** 在 AI 建议面板顶部增加"全部标记删除"按钮，一键将所有建议转为删除标记。用户随后可在时间轴中手动取消不想删的部分。

- **优先级：** 🟢 低
- **改动范围：** 前端 `app.js` + `index.html`

---

### 1.6 手动添加删除标记

**现状：** AI 建议覆盖不全时（如 AI 漏掉了某处明显口误），用户无法手动在"AI 建议"面板中添加自己的标记，只能去时间轴手动拖拽创建删除范围。但对于不熟悉时间轴操作的用户，直接标记文字更方便。

**建议：** 在"识别全文"标签页中支持拖选一段文字 → 右键菜单 → "标记删除"，自动创建对应时间范围的删除标记并同步到 AI 建议面板和剪切时间轴。

- **优先级：** 🟡 中
- **改动范围：** 前端 `app.js`

---

## 二、视频播放体验

### 2.1 倍速播放

**现状：** 所有视频播放器（主预览、剪切预览、艺术字预览、画中画预览）均不支持倍速。用户在审阅长视频时无法加速浏览。

**建议：** 给所有 `<video>` 播放器添加倍速选择器（0.5× / 0.75× / 1× / 1.25× / 1.5× / 2×），利用浏览器原生的 `video.playbackRate` 属性即可实现，无需服务端参与。

- **优先级：** 🔴 高
- **改动范围：** 前端所有 HTML 页面 + JS + CSS

---

### 2.2 键盘快捷键

**现状：** 整个应用几乎没有任何键盘快捷键，所有操作依赖鼠标点击。

**建议：** 增加以下快捷键：

| 快捷键 | 功能 |
|--------|------|
| `Space` | 播放 / 暂停 |
| `←` / `→` | 后退 / 前进 5 秒 |
| `Shift + ←` / `Shift + →` | 后退 / 前进 1 帧 |
| `Ctrl+Z` / `Ctrl+Y` | 撤销 / 重做（依赖 1.1） |
| `Delete` | 删除当前选中的词段 / 范围 |
| `Escape` | 取消当前选择 / 关闭弹窗 |
| `M` | 静音 / 取消静音 |
| `F` | 全屏 |

- **优先级：** 🟡 中
- **改动范围：** 前端所有 JS 文件

---

### 2.3 快进 / 快退按钮

**现状：** 视频播放器只有播放/暂停、进度条、音量、全屏，没有快速跳转按钮。用户想回看某句话需要拖动进度条，不够精准。

**建议：** 在播放/暂停按钮两侧增加快退 5s 和快进 5s 的按钮，这是视频编辑工具的标配交互。

- **优先级：** 🟡 中
- **改动范围：** 前端 HTML + JS + CSS（所有播放器）

---

## 三、导出与下载

### 3.1 导出字幕文件（SRT / VTT）

**现状：** 系统已有词级别的时间戳数据（精确到毫秒），但没有字幕导出功能。用户如果需要给视频配上字幕，只能手动制作。

**建议：** 在"生成结果"标签页增加"下载字幕"按钮，支持 SRT 和 VTT 两种格式。按句子级别生成字幕条目（利用现有的 `sentences` 数据结构），每句一条，自动编号并格式化时间戳。这是非常低成本但高价值的功能——数据已经齐备，只是格式化输出的问题。

- **优先级：** 🔴 高
- **改动范围：** 后端 `app.py`（新增一个导出接口）+ 前端按钮

---

### 3.2 导出纯音频（MP3）

**现状：** 系统内部已经提取了音频（`speech.mp3`），且剪切后的视频也包含对应音频。但用户无法单独下载音频文件——有时用户只需要播客/音频版本。

**建议：** 在"生成结果"标签页增加"下载音频（MP3）"按钮，直接提供已提取的原始音频下载。如果用户做了剪切，额外提供一个"下载剪切后音频"选项。

- **优先级：** 🟡 中
- **改动范围：** 后端 `app.py`（新增一个流式文件接口）

---

### 3.3 导出 TXT 转录稿

**现状：** "识别全文"标签页已有"复制"和"下载 TXT"按钮。但导出的是原始转写全文，没有考虑删除操作——用户可能想要导出剪切后保留的文字稿。

**建议：** 在"生成结果"标签页也放置一个"下载保留文字稿"按钮，导出剪切后保留的文本段落（基于 `retained_transcript` 数据）。

- **优先级：** 🟢 低
- **改动范围：** 后端 `app.py` + 前端

---

## 四、画中画（PiP）体验

### 4.1 画中画淡入淡出过渡

**现状：** 画中画素材（图片/视频）在时间轴上直接出现和消失，没有过渡效果，视觉上比较生硬。

**建议：** 在画中画确认面板中增加"淡入"和"淡出"时长设置（默认 0.3s）。FFmpeg 侧通过 `fade` 滤镜实现即可，改动很小。

- **优先级：** 🟡 中
- **改动范围：** 前端 `picture-in-picture.html` + `picture-in-picture.js` + 后端 `app.py`（`render_picture_in_picture_video`）

---

### 4.2 多个画中画的层级管理

**现状：** 如果用户在同一时间段添加了多个画中画素材，后添加的覆盖先添加的。但目前没有层级调整的 UI。

**建议：** 在画中画素材卡片上增加"上移一层"/"下移一层"按钮，调整 FFmpeg overlay 的顺序。

- **优先级：** 🟢 低
- **改动范围：** 前端 + 后端

---

### 4.3 画中画尺寸的自定义数值输入

**现状：** 画中画尺寸通过滑块调整（20%-100%），但没有精确数值输入框。用户无法精确控制素材大小（如设为视频宽度的 33%）。

**建议：** 在滑块旁边增加一个百分比数字输入框，与滑块双向联动。

- **优先级：** 🟢 低
- **改动范围：** 前端

---

## 五、艺术字体验

### 5.1 艺术字淡入淡出

**现状：** 与画中画类似，艺术字叠加上去后直接出现/消失，没有过渡。

**建议：** 在艺术字设置面板中增加"淡入"和"淡出"时长（与画中画 4.1 保持一致的设计）。FFmpeg 侧通过 `fade` 滤镜应用于 overlay 的 alpha 通道。

- **优先级：** 🟡 中
- **改动范围：** 前端 `art-text.html` + `art-text.js` + 后端 `app.py`

---

### 5.2 AI 艺术字建议可手动增删改

**现状：** AI 生成的艺术字建议只能确认或拒绝，不能修改文字内容、位置或样式参数后确认。如果 AI 建议的文字方向对但措辞不完美，用户无法微调。

**建议：** AI 建议卡片上的文字、位置、样式都改为可编辑状态，修改后可以直接确认（无须先拒绝再手动创建）。

- **优先级：** 🟢 低
- **改动范围：** 前端 `art-text.js`

---

### 5.3 保存自定义艺术字模板

**现状：** 用户调整了某个艺术字的字体、颜色、描边、阴影等参数后，无法保存为自定义模板以便在其他视频中复用。目前只有 11 个内置模板。

**建议：** 在艺术字设置面板增加"保存为我的模板"按钮，将当前参数存到 `localStorage`。在模板面板中增加"我的模板"分类。

- **优先级：** 🟢 低
- **改动范围：** 前端（不需要后端改动）

---

## 六、数据持久化与容错

### 6.1 任务状态简单持久化

**现状：** 所有任务状态仅存于服务器内存中（`JOBS` 字典）。服务重启后，所有进行中/已完成的任务记录丢失。虽然磁盘上的 `data/jobs/<uuid>/` 文件还在，但无法通过 API 再次访问。用户在编辑过程中刷新页面或服务重启，之前的工作全部丢失。

**建议：** MVP 阶段不用上 SQLite，但可以在每个 job 目录下维护一个 `state.json` 文件，在关键节点（ASR 完成、剪切完成、艺术字生成完成等）写入当前状态。服务启动时扫描 `data/jobs/` 目录恢复 `JOBS` 字典。这样服务重启后任务可恢复，且不增加新依赖。

- **优先级：** 🔴 高
- **改动范围：** 后端 `app.py`（约 100-150 行新增代码）

---

### 6.2 轮询失败自动重试

**现状：** 前端通过 `setInterval` 每秒轮询任务状态。如果网络暂时断开或服务端短暂不可用，轮询静默失败，进度条卡住不动，用户不知道发生了什么。

**建议：** 在轮询逻辑中增加连续失败计数。连续失败 3 次后显示"连接中断，正在重试…"提示，并自动继续重试；连续失败 10 次后显示"请检查网络后刷新页面"。同时增加指数退避（重试间隔从 1s 逐步增加到 5s）。

- **优先级：** 🟡 中
- **改动范围：** 前端 `app.js` + `art-text.js` + `picture-in-picture.js`

---

### 6.3 大文件上传的断点续传

**现状：** 视频上传使用 XMLHttpRequest，不支持断点续传。上传 1GB 视频时如果网络中断，必须从头开始。

**建议：** 将上传改为分片上传（每片 10MB）。服务端增加两个接口：`POST /api/transcriptions/{job_id}/chunks` 接收分片，`POST /api/transcriptions/{job_id}/chunks/complete` 合并分片。前端在 `localStorage` 记录已上传的分片编号，中断后可续传。这是 MVP 到生产之间差距最大的体验问题之一。

- **优先级：** 🟡 中
- **改动范围：** 前端 `app.js` + 后端 `app.py`

---

## 七、视频内容结构化

### 7.1 段落自动分割与折叠

**现状：** 转写结果是一片连续的文字加时间戳，没有段落结构。对于 10 分钟以上的口播视频，用户很难快速定位到某个话题段落。

**建议：** 在标点润色阶段，利用 Qwen LLM 同时输出语义段落分割（按话题/语意转换点）。前端在"识别全文"标签页中以段落卡片形式展示，可折叠/展开，每段显示起始时间戳。段落标题可由 LLM 自动生成（一句话概括）。

- **优先级：** 🟡 中
- **改动范围：** 后端 `app.py`（punctuation polishing 阶段扩展 prompt）+ 前端

---

### 7.2 按段落快速选择保留/删除

**现状：** 只能按词段粒度选择删除。如果某个话题跑题了想整段删掉，需要手动从时间轴上拖拽一个大范围。

**建议：** 在段落视图（参见 7.1）中，每个段落卡片左侧增加复选框。勾选 = 保留，取消勾选 = 整段删除。这比逐词选择高效得多，尤其适合长视频粗剪。

- **优先级：** 🟡 中
- **改动范围：** 前端 + 后端

---

## 八、杂项小改进

### 8.1 视频首尾裁剪（Trim）

**现状：** 系统只能删除中间的片段，不能裁剪视频的开头（如录制开始时的准备动作）和结尾（如录制结束后的多余画面）。用户必须在时间轴上手动创建从 0 开始的删除范围，不够直观。

**建议：** 在时间轴两端增加"入点"和"出点"手柄（类似传统视频编辑器的 trim 手柄），拖动即可设置视频的实际起始和结束位置。

- **优先级：** 🟡 中
- **改动范围：** 前端 + 后端

---

### 8.2 字体管理支持预览文本自定义

**现状：** 字体管理页面有预览文本输入框但默认值是固定文本"新征程新赶考"，字体详情面板在用户未手动修改预览文本时也使用这个默认值。

**建议：** 在字体详情面板增加"使用当前转写文本预览"按钮，一键将视频的转写文字片段填入预览区，让用户看到字体在实际内容中的效果。

- **优先级：** 🟢 低
- **改动范围：** 前端 `font-manager.js`

---

### 8.3 页面标题动态更新

**现状：** 所有页面的 `<title>` 是静态的。用户在多个标签页中打开不同任务时，无法通过标题区分。

**建议：** 在主编辑页面将 `<title>` 动态更新为 `"编辑 - <文件名>"` 或 `"编辑 - <job_id 前8位>"`。

- **优先级：** 🟢 低
- **改动范围：** 前端 `app.js`

---

### 8.4 长时间处理的进度细化

**现状：** 后台任务（ASR 转写、视频渲染、AI 建议生成）的进度条只有 3-4 个阶段，每个阶段可能需要数十秒到数分钟，用户在这段时间内看到的是一条不动的进度条。

**建议：**
- **ASR 阶段**：如果可以获取音频时长，显示"转写中… 已处理 30s / 总共 120s"
- **视频渲染阶段**：FFmpeg 输出中可解析 `time=` 行来推算百分比进度
- **AI 建议生成**：至少显示"正在分析…"直到返回
- 对 Seedance 异步轮询，显示已等待时间和轮询次数

- **优先级：** 🟢 低
- **改动范围：** 后端（推送更细粒度的进度）+ 前端

---

### 8.5 删除范围的颜色标记

**现状：** 时间轴上所有删除范围都是同一种红色。用户无法区分"AI 建议标记的删除"和"手动标记的删除"。

**建议：** 用不同颜色区分：AI 建议标记的用橙色，手动创建的用红色。在删除范围上悬浮时显示来源标签。

- **优先级：** 🟢 低
- **改动范围：** 前端 CSS + JS

---

## 建议优先级汇总

| 优先级 | 编号 | 建议 |
|--------|------|------|
| 🔴 高 | 1.1 | 撤销 / 重做 |
| 🔴 高 | 1.2 | 手动修正 ASR 转录文字 |
| 🔴 高 | 2.1 | 倍速播放 |
| 🔴 高 | 3.1 | 导出字幕文件（SRT / VTT） |
| 🔴 高 | 6.1 | 任务状态简单持久化（state.json） |
| 🟡 中 | 1.3 | 删除范围精确时间输入 |
| 🟡 中 | 1.4 | 提交前本地预览跳过区间 |
| 🟡 中 | 1.6 | 手动添加删除标记 |
| 🟡 中 | 2.2 | 键盘快捷键 |
| 🟡 中 | 2.3 | 快进 / 快退按钮 |
| 🟡 中 | 3.2 | 导出纯音频（MP3） |
| 🟡 中 | 4.1 | 画中画淡入淡出 |
| 🟡 中 | 5.1 | 艺术字淡入淡出 |
| 🟡 中 | 6.2 | 轮询失败自动重试 |
| 🟡 中 | 6.3 | 大文件分片上传 |
| 🟡 中 | 7.1 | 段落自动分割 |
| 🟡 中 | 7.2 | 按段落快速选择保留/删除 |
| 🟡 中 | 8.1 | 视频首尾裁剪（Trim） |
| 🟢 低 | 1.5 | 一键应用全部 AI 建议 |
| 🟢 低 | 3.3 | 导出保留文字稿 |
| 🟢 低 | 4.2 | PiP 层级管理 |
| 🟢 低 | 4.3 | PiP 尺寸数值输入 |
| 🟢 低 | 5.2 | AI 艺术字建议可手动修改 |
| 🟢 低 | 5.3 | 保存自定义艺术字模板 |
| 🟢 低 | 8.2 | 字体预览使用转写文本 |
| 🟢 低 | 8.3 | 页面标题动态更新 |
| 🟢 低 | 8.4 | 长时间处理进度细化 |
| 🟢 低 | 8.5 | 删除范围颜色区分来源 |
