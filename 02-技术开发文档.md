# AI 口播视频剪辑 Web MVP 技术开发文档

| 项目 | 内容 |
|---|---|
| 文档版本 | MVP V1.0 |
| 架构原则 | 单体应用、单服务器、优先打通闭环 |
| 建议周期 | 4–6 周 |

### 当前已落地的轻量版本

为尽快验证核心闭环，当前代码使用 FastAPI、原生 HTML/CSS/JavaScript、内存任务状态和本地文件目录，不引入 React、SQLite 或独立 Worker。上传、百炼 Paraformer 转写、词级选择、后台 FFmpeg 剪辑、成片预览与下载均已可用。下文中的 React、SQLite 和独立 Worker 是有真实用户量后再演进的目标架构。

## 1. 技术范围

MVP 只实现一条完整链路：

```text
上传视频 → 生成代理 → 语音转写 → 文字删减 → 添加简单视觉元素 → FFmpeg 导出
```

不在首版引入微服务、Kubernetes、复杂工作流引擎、多区域部署、实时协作和模板平台。代码需要保留基本模块边界，但不为未来场景提前建设复杂基础设施。

## 2. 建议技术栈

| 模块 | 建议技术 | 用途 |
|---|---|---|
| 前端 | React + TypeScript + Vite | 上传、文本编辑、视频预览和设置面板 |
| UI 状态 | Zustand | 保存当前工程和编辑状态 |
| 文本编辑 | 普通 React 组件或轻量编辑器 | MVP 不需要完整富文本框架 |
| 视频预览 | HTMLVideoElement | 播放 720p 代理视频 |
| 后端 | Python + FastAPI | 文件、工程、AI 调用和导出 API |
| 后台任务 | 一个独立 Python Worker | 顺序执行转写、分析、图片生成和导出任务 |
| 数据库 | SQLite | 保存工程、任务状态和文件引用 |
| 文件存储 | 服务器数据目录 | 保存原视频、代理、图片和导出文件 |
| 视频处理 | FFmpeg + FFprobe | 探测、代理、切割、叠加和编码 |
| 艺术字图片 | Pillow | 根据固定模板生成透明 PNG |
| AI | 阿里云百炼 Paraformer ASR、一个 LLM、一个图片服务 | 通过简单适配函数调用第三方 API |
| 部署 | 单台云服务器 + Nginx + Docker Compose | 部署前端、API 和 Worker |

如果服务器磁盘不够，再将视频文件换成对象存储；MVP 第一版不必同时实现两套存储。

## 3. 简化架构

```text
┌──────────────────── 浏览器 ────────────────────┐
│ React：上传、转写文本、预览、模板选择、导出状态 │
└─────────────────────┬──────────────────────────┘
                      │ HTTP API
                      ▼
┌──────────────── FastAPI 单体后端 ───────────────┐
│ 工程 API │ 文件 API │ AI 调用封装 │ 任务管理     │
└──────────────┬──────────────────┬───────────────┘
               │                  │
               ▼                  ▼
        SQLite + 项目目录     Python Worker
                              FFmpeg / ASR / LLM / 图片生成
```

### 3.1 前端职责

- 上传文件并显示进度。
- 展示转写文字和 AI 建议。
- 将文字选择转换为 token ID，提交删除或恢复操作。
- 根据 EDL 控制视频在预览时跳过已删除区间。
- 以 HTML 图层模拟艺术字和画中画预览。
- 显示后台任务状态和导出下载链接。

### 3.2 FastAPI 职责

- 创建和读取工程。
- 接收视频并流式写入磁盘，限制格式与大小。
- 保存文字编辑、模板和效果配置。
- 创建后台任务并返回任务 ID。
- 读取任务进度和结果。
- 使用随机工程 ID 校验文件访问，不接受客户端提交任意服务器路径。

### 3.3 Worker 职责

- 每次从 SQLite 领取一个待处理任务。
- 执行 FFprobe、FFmpeg、ASR、LLM 或图片服务调用。
- 定期更新任务阶段和进度。
- 成功后写入结果；失败后保存可读错误。
- MVP 可以只运行一个 Worker，避免并发资源争抢。

## 4. 项目结构

```text
repo/
├─ web/                         # React 前端
│  └─ src/
│     ├─ pages/
│     ├─ components/
│     ├─ editor/
│     └─ api/
├─ server/
│  ├─ app.py                    # FastAPI 入口
│  ├─ api/                      # 工程、上传、编辑、任务 API
│  ├─ services/                 # ASR、LLM、图片和 FFmpeg 封装
│  ├─ worker.py                 # 单 Worker 入口
│  ├─ models.py                 # SQLite 模型
│  └─ schemas.py                # API 数据结构
├─ data/                        # 开发环境数据目录
├─ templates/                   # 十一种艺术字模板及字体
├─ tests/
├─ docker-compose.yml
└─ .env.example
```

## 5. 数据设计

MVP 只需要两张主表和一个工程 JSON。

### 5.1 projects 表

| 字段 | 说明 |
|---|---|
| id | 随机 UUID |
| user_id | 简单用户 ID；内部版可固定为一个值 |
| name | 工程名称 |
| status | uploaded/processing/ready/exporting/failed |
| source_path | 原视频相对路径 |
| proxy_path | 代理视频相对路径 |
| project_json_path | 工程 JSON 路径 |
| created_at/updated_at | 创建和更新时间 |

### 5.2 jobs 表

| 字段 | 说明 |
|---|---|
| id | 任务 UUID |
| project_id | 所属工程 |
| type | prepare/transcribe/analyze/image/export |
| status | queued/running/succeeded/failed |
| progress | 0–100 |
| error_message | 可展示的失败原因 |
| result_json | 任务结果摘要 |
| created_at/updated_at | 时间 |

### 5.3 project.json

```json
{
  "version": 1,
  "source": {
    "durationUs": 600000000,
    "width": 1920,
    "height": 1080,
    "fps": 30
  },
  "tokens": [],
  "removedRanges": [],
  "suggestions": [],
  "artTexts": [],
  "pipImages": [],
  "effectPreset": "natural"
}
```

工程写入时先写临时文件，再替换原 JSON，并保留一个 `.backup` 文件。

## 6. 核心数据结构

### 6.1 转写 token

```ts
type Token = {
  id: string;
  text: string;
  startUs: number;
  endUs: number;
  confidence?: number;
  deleted: boolean;
};
```

用户修改 `text` 只修正显示内容；只有 `deleted` 状态变化才影响视频。

### 6.2 删除区间

```ts
type TimeRange = {
  startUs: number;
  endUs: number;
};
```

所有时间统一使用整数微秒，避免浮点秒造成字幕和切点漂移。

### 6.3 艺术字

```ts
type ArtText = {
  id: string;
  text: string;
  template: "title" | "keyword" | "number";
  startUs: number;
  endUs: number;
  position: "top" | "center" | "bottom";
  color: string;
};
```

### 6.4 画中画

```ts
type PipImage = {
  id: string;
  imagePath: string;
  prompt: string;
  startUs: number;
  endUs: number;
  position: "top-left" | "top-right" | "bottom-left" | "bottom-right";
  scale: number;
};
```

## 7. 关键实现流程

### 7.1 上传和准备

1. 前端使用 `multipart/form-data` 上传视频。
2. FastAPI 流式写入临时文件，不将完整文件读入内存。
3. 校验文件大小、扩展名并使用 FFprobe 验证真实媒体格式。
4. 文件移动到 `data/projects/{projectId}/source/`。
5. 创建 `prepare` 任务。
6. Worker 生成 720p H.264 代理、16kHz 单声道 MP3 和基础缩略图。
7. 准备完成后自动创建转写任务。

文件夹建议：

```text
data/projects/{projectId}/
├─ source/input.mp4
├─ proxy/preview.mp4
├─ audio/speech.mp3
├─ generated/
├─ export/final.mp4
├─ project.json
└─ project.json.backup
```

### 7.2 语音转写

1. Worker 将 MP3 发送给阿里云百炼 `paraformer-realtime-v2` ASR 服务。
2. 开启 `semantic_punctuation_enabled`，使用语义断句代替默认 VAD 断句。
3. 要求返回词级时间戳；如果只能得到句级时间戳，MVP 暂停上线字级删除功能。
4. 将去除标点后的连续正文交给 `qwen-plus` 做二次标点校对；提示词禁止修改正文字符。
5. 校验校对前后的汉字、字母和数字完全一致，再将新标点映射回原词级时间戳；校验失败时自动使用 Paraformer 原结果。
6. 根据句号、问号和感叹号重新生成段落，全文按句换行展示。

只实现一个 ASR Provider：

```py
async def transcribe(audio_path: str) -> list[Token]:
    ...
```

### 7.3 文本删除算法

1. 获取用户选中的所有有效 token。
2. 单词使用自身起止时间；整段选择使用句段起止时间，从而一并删除段内停顿。
3. 将重叠或间隔不超过 30ms 的删除区间合并。
4. 校验时间为有限数字、起止顺序正确、区间位于源视频时长内，并禁止删除整段视频。
5. 求删除区间的补集，得到最终保留区间。
6. FFmpeg 对每个保留区间分别执行 `trim/atrim` 和时间戳归零，再使用 `concat` 拼接音视频。
7. 输出 H.264/AAC MP4，先写临时文件，成功后再替换旧成片；源视频不覆盖。

```ts
function buildRemovedRange(tokens: Token[]): TimeRange {
  return {
    startUs: Math.max(0, tokens[0].startUs - 50000),
    endUs: tokens[tokens.length - 1].endUs + 80000
  };
}
```

当前版本不自动扩展词语边界，避免吞掉相邻发音；具体缓冲和短音频淡化需要用实际口播样片验证后再加入。

当前接口：

```text
POST /api/transcriptions/{job_id}/cuts
GET  /api/transcriptions/{job_id}
GET  /api/transcriptions/{job_id}/edited-video
```

### 7.4 浏览器剪辑预览

MVP 不实时重新生成视频。前端维护保留区间列表：

- 播放进入已删除区间时，立即跳转到下一个保留区间。
- 当前艺术字和画中画使用绝对定位 HTML 图层显示。
- 滤镜使用 CSS 做近似预览。
- 页面标注“最终导出效果可能有轻微差异”。

### 7.5 AI 删减建议

- 语气词：使用固定词表匹配。
- 长停顿：根据相邻 token 的时间差判断。
- 重复：使用相邻句子相似度或简单文本规则。
- 口误纠正：将带 token ID 的小段文本发送给 LLM，要求返回严格 JSON。

LLM 只能输出 token ID、建议类型和原因，不能输出文件路径、命令或 FFmpeg 参数。

### 7.6 艺术字

1. LLM 从保留文本中最多选出五个标题、关键词或数字。
2. 用户确认模板、文字、颜色和位置。
3. Pillow 使用固定字体和模板生成透明 PNG。
4. 导出时 FFmpeg 在指定时间叠加 PNG。
5. 动画只实现透明度淡入淡出；缩放效果作为可选增强。

字体必须使用明确可商用的中文字体。

### 7.7 AI 图片画中画

1. LLM 最多选择三个适合配图的片段并生成简短图片提示词。
2. 用户点击生成后调用一个图片服务。
3. 图片保存到工程 `generated/` 目录。
4. 前端显示四角位置和缩放选项。
5. 导出时 FFmpeg 使用 overlay 滤镜叠加，并增加简单淡入淡出。

### 7.8 滤镜和动态效果

使用固定参数，不做复杂 AI 调色：

```text
off     = 不处理
natural = 轻微对比度、饱和度和锐度提升
vivid   = 更明显的对比度、饱和度提升
```

动态效果只实现以下两种：

- 长片段缓慢从 1.00 放大到约 1.05。
- 章节边界使用短淡入淡出。

所有参数写死在服务端白名单中，不允许 LLM 生成 FFmpeg 命令。

### 7.9 最终导出

1. 校验源视频、工程 JSON、字体和图片是否存在。
2. 根据删除区间计算保留区间。
3. FFmpeg 截取并拼接保留视频。
4. 添加短音频交叉淡化。
5. 应用一个效果预设。
6. 叠加艺术字 PNG 和画中画图片。
7. 输出固定为 1080p MP4/H.264/AAC。
8. 更新导出任务并提供下载地址。

MVP 先追求结果正确，不要求导出速度达到实时。

## 8. 最小 API

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/projects` | 创建工程 |
| GET | `/api/projects` | 最近工程列表 |
| GET | `/api/projects/{id}` | 获取工程 |
| POST | `/api/projects/{id}/video` | 上传视频 |
| PATCH | `/api/projects/{id}/edit` | 保存删除、艺术字、画中画和效果配置 |
| POST | `/api/projects/{id}/analyze` | 创建删减和视觉建议任务 |
| POST | `/api/projects/{id}/images` | 创建图片生成任务 |
| POST | `/api/projects/{id}/export` | 创建导出任务 |
| GET | `/api/jobs/{id}` | 查询任务进度与结果 |
| GET | `/api/files/{projectId}/{fileId}` | 受控读取预览或导出文件 |
| DELETE | `/api/projects/{id}` | 删除工程及文件 |

前端每 1–2 秒轮询任务状态即可，MVP 不必引入 WebSocket。

## 9. 任务处理

### 9.1 状态

```text
queued → running → succeeded
                 └→ failed
```

### 9.2 Worker 简化规则

- 每次只领取一个任务。
- `running` 任务超过设定时间未更新，则启动时重置为 `queued`。
- 导出、转写和生成图片失败最多自动重试一次。
- 用户重复点击导出时，如果已有进行中的导出任务，直接返回原任务 ID。
- 任务异常不得破坏上一个有效的 project.json。

## 10. 安全底线

- 上传大小、扩展名和实际媒体格式都要校验。
- 文件名由服务端重新生成，不能直接使用用户文件名作为路径。
- 所有路径必须限制在当前工程目录内。
- 调用 FFmpeg 使用参数数组，禁止拼接用户输入执行 shell。
- API 密钥只放服务端环境变量，不返回浏览器。
- 视频文件不放在公开静态目录，通过校验工程归属的接口读取。
- 如果开放给外部用户，必须增加简单登录并校验 project.user_id。
- 日志不记录完整转写文本、图片提示词或 API 密钥。

## 11. 错误处理

| 错误 | 处理 |
|---|---|
| 上传中断 | 用户重新上传；断点续传留到后续版本 |
| 视频无法解码 | 显示“不支持或文件损坏” |
| ASR 失败 | 保留工程并提供重试按钮 |
| LLM 分析失败 | 用户仍可手动删除文字和添加元素 |
| 图片生成失败 | 允许重试或跳过，不阻断导出 |
| FFmpeg 导出失败 | 保留临时日志并允许重试一次 |
| 页面刷新 | 从服务端重新加载 project.json 和任务状态 |
| 磁盘不足 | 停止新任务并显示管理员错误信息 |

## 12. 测试清单

### 12.1 必测功能

- 上传 MP4、MOV 和错误格式文件。
- 5 分钟、10 分钟、30 分钟视频。
- 普通话、少量英文、背景噪声和长停顿。
- 修改错字与删除文字的区别。
- 连续删除、跨句删除、撤销和恢复。
- 三种艺术字、四个画中画位置和三档效果。
- 页面刷新后的工程恢复。
- 图片、ASR、LLM 和导出分别失败的降级。

### 12.2 媒体检查

- 竖屏 1080×1920 和横屏 1920×1080。
- 25fps、30fps 和常见手机可变帧率素材。
- 检查切点是否吞字、爆音或出现明显黑帧。
- 检查导出字幕/艺术字时间和画中画时间。
- 检查最终音画同步误差不超过 2 帧。

### 12.3 最小自动化测试

- 时间区间合并和求补集单元测试。
- token 到删除区间映射测试。
- 工程 JSON 读写和备份恢复测试。
- 一个固定 1 分钟样片的端到端导出测试。
- API 不能跨用户读取工程文件的权限测试。

## 13. 部署方案

MVP 使用一台云服务器：

```text
Nginx
├─ /             → React 静态文件
└─ /api          → FastAPI

Docker Compose
├─ api
├─ worker
└─ shared data volume
```

SQLite 和项目文件挂载到持久化磁盘，每天备份一次。上线前配置 HTTPS、域名、磁盘监控和服务自动重启。

建议初始服务器至少具备：

- 4–8 核 CPU。
- 16GB 内存。
- 足够保存测试素材的磁盘空间。
- GPU 不是必须；ASR 和图片生成优先调用云 API。

## 14. 环境变量

```text
APP_SECRET=
DATA_DIR=/data/projects
MAX_UPLOAD_MB=1024
DASHSCOPE_API_KEY=
LLM_API_KEY=
IMAGE_API_KEY=
FONT_PATH=/app/templates/font.ttf
```

`.env` 不提交 Git，仓库只保留 `.env.example`。

## 15. 开发顺序

### 第 1 周：最小媒体闭环

- 创建工程、上传视频、FFprobe 探测。
- 生成 720p 代理和音频。
- 调用 ASR 并展示文字。

### 第 2 周：文字剪辑

- token 与视频跳转。
- 删除、恢复、撤销和预览跳过。
- FFmpeg 根据保留区间导出视频。

### 第 3 周：AI 建议与艺术字

- 语气词、长停顿、重复和口误建议。
- 十一种艺术字模板、Pillow PNG 和 FFmpeg 叠加。

### 第 4 周：AI 图片与效果

- 图片建议、生成、四角位置和叠加。
- 三档滤镜、轻微推近和淡入淡出。
- 完整页面流程和工程保存。

### 第 5–6 周：可选质量收口

- 异常重试、备份恢复和删除工程。
- 真实口播样片调参。
- 导出性能、浏览器兼容和部署。

## 16. 团队建议

| 角色 | 人数 | 主要工作 |
|---|---:|---|
| Web 前端 | 1 | 上传、文本编辑、预览和设置面板 |
| Python/FFmpeg 后端 | 1 | API、Worker、AI 调用和渲染 |
| UI/测试 | 0.5–1 | 三种模板、交互和验收测试 |

如果只有一名全栈开发，应先完成“上传—转写—删文字—导出”，再按顺序加入艺术字、AI 图片和效果。

## 17. MVP 完成定义

满足以下条件即可进入真实用户测试：

- 用户能上传一段普通话口播视频。
- 系统能生成带词级时间戳的转写文本。
- 用户能通过删除文字删掉对应视频。
- 系统能推荐并添加三种艺术字中的至少一种。
- 系统能生成并插入至少一张 AI 画中画图片。
- 用户能选择自然或活力效果。
- 系统能成功导出并下载 1080p MP4。
- 页面刷新、单项 AI 失败或导出重试不会丢失工程。
