# AI 口播视频剪辑 Web MVP 产品需求文档

| 项目 | 内容 |
|---|---|
| 文档版本 | MVP V1.0 |
| 产品形态 | Web 应用 |
| 目标用户 | 个人口播短视频创作者 |
| 核心目标 | 通过修改文字完成视频粗剪，并快速增加简单视觉包装 |
| 建议周期 | 4–6 周 |

## 1. 产品目标

用户上传一段中文口播视频后，系统自动完成语音转文字并标记可删内容。用户像编辑文档一样删除文字，对应视频片段同步删除；系统再根据保留内容添加少量艺术字、AI 图片画中画和轻量效果，用户确认后导出成片。

MVP 只验证以下四项需求是否真正节省剪辑时间：

1. 语音转文字，并通过删文字完成视频粗剪。
2. 自动推荐艺术字，用户可以选择固定模板。
3. 根据内容生成少量 AI 图片并加入画中画。
4. 自动增加克制的滤镜和动态效果。

## 2. 目标用户与场景

### 2.1 目标用户

- 不熟悉专业剪辑软件的口播创作者。
- 需要频繁剪辑知识分享、产品介绍或个人观点视频的用户。
- 视频以单人、普通话、固定机位为主。

### 2.2 典型场景

用户录制了一段 10 分钟口播，其中包含口误、重复表达、语气词和停顿。用户上传视频，接受部分 AI 删除建议，再手动删除几句话，添加三处关键词艺术字和两张辅助图片，最后导出一段约 7 分钟的视频。

## 3. MVP 范围

### 3.1 本期必须完成

| 编号 | 功能 | 优先级 |
|---|---|---:|
| F-01 | 视频上传与工程创建 | P0 |
| F-02 | 中文语音识别和文字展示 | P0 |
| F-03 | 删除文字同步删除视频 | P0 |
| F-04 | 口误、语气词、重复和长停顿建议 | P0 |
| F-05 | 十一种艺术字模板及自动推荐 | P0 |
| F-06 | 根据内容生成 AI 图片并加入画中画 | P0 |
| F-07 | 简单滤镜、推近和淡入淡出 | P0 |
| F-08 | 浏览器预览、工程保存和 MP4 导出 | P0 |

### 3.2 本期明确不做

- 不做专业多轨时间线。
- 不做多个视频拼接、复杂转场和背景音乐库。
- 不做多人实时协作、团队空间和模板商城。
- 不做复杂会员、订单和按量计费系统。
- 不做方言、多人访谈、多语言和说话人识别。
- 不做手机端完整剪辑。
- 不做 Premiere、Final Cut 或 DaVinci 工程导出。
- 不做用户自由设计复杂模板；只允许选择和修改少量属性。
- 不追求完全自动成片，关键删除和 AI 图片由用户确认。

## 4. 核心流程

1. 用户进入网页并创建工程。
2. 上传一段 MP4/MOV 口播视频。
3. 系统生成预览视频并完成语音转写。
4. 页面显示全文和 AI 删减建议。
5. 用户接受建议或直接删除文字，预览时自动跳过被删片段。
6. 系统根据保留内容推荐艺术字、AI 图片和轻量效果。
7. 用户接受、替换或关闭视觉元素。
8. 用户发起导出，等待服务端渲染完成后下载 MP4。

## 5. 详细功能需求

### 5.1 F-01 视频上传与工程创建

- 每个工程只允许上传一段视频。
- 支持 MP4、MOV，建议限制为最长 30 分钟、最大 1GB。
- 上传页面显示进度、成功和失败状态。
- 上传完成后自动生成 720p 预览视频。
- 文件无法解码时显示明确错误，不进入后续处理。
- MVP 可使用简单邮箱登录；如果只做内部演示，也可暂时使用固定测试账号。
- 工程自动保存，刷新页面后可以继续编辑。

### 5.2 F-02 语音识别

- 上传完成后自动提取音频并调用一个语音识别服务。
- MVP 默认使用阿里云百炼 `paraformer-realtime-v2`，API Key 只保存在服务端。
- 仅支持中文普通话，允许少量常见英文词。
- 识别结果至少包含句子、词语和对应起止时间。
- 页面按段落展示文字，播放时高亮当前句子。
- 点击文字，视频跳转至对应位置。
- 用户可以修改错别字；修改文字不改变视频时间。
- 识别失败时允许重新执行。

### 5.3 F-03 文字删减与视频联动

- 用户可以按词选择，或一键选择整段文字，然后执行“生成剪辑视频”。
- 待删除文字显示红色删除线，再次点击可在生成前恢复。
- 生成前显示预计删除时长并二次确认，防止误操作。
- 系统根据词语时间戳生成删除区间，相邻删除区间自动合并。
- 系统生成新的 MP4 成片并提供预览、下载，原视频始终保留。
- 剪辑失败显示可读错误，用户可重新选择文字再次生成。
- MVP 不提供精细到帧的传统时间线，只提供文本编辑和当前片段时间显示。

本阶段暂不实现撤销历史、拖拽框选、实时跳切预览和精细切点调整；用户通过重新选择文字并再次生成完成修改。

### 5.4 F-04 AI 删减建议

系统只给出以下四类建议：

- 常见语气词，如“嗯、啊、就是、然后”。
- 明显长停顿。
- 相邻重复词句。
- 明显口误和紧接着的自我纠正。

每条建议显示对应文字、理由和“接受/忽略”按钮。AI 默认不直接删除完整观点或长段内容，避免改变原意。

### 5.5 F-05 艺术字

MVP 只提供三类固定模板：

1. 顶部标题：用于章节标题或结论。
2. 关键词强调：用于重点词语。
3. 数字卡片：用于金额、比例、日期或步骤数。

功能要求：

- 系统从保留内容中推荐标题、关键词和数字。
- 用户可接受或忽略建议，也可手动添加艺术字。
- 用户可以在三种模板中切换。
- 用户可修改文字、主色、出现时间、持续时间和上/中/下位置。
- 入场和出场动画只做淡入淡出；可选一个简单缩放动画。
- 默认每分钟不超过一处艺术字，避免画面过满。

### 5.6 F-06 AI 图片画中画

- 系统从保留内容中选择最多三处适合用图片解释的内容。
- 每一处先展示建议主题，用户确认后才调用图片生成服务。
- 每次先生成一张图片，用户可以接受、重新生成或跳过。
- 图片插入对应片段，默认持续 3–5 秒。
- 画中画只提供左上、右上、左下、右下四个位置。
- 用户可修改位置、大小和持续时间。
- 动画只做淡入淡出或轻微缩放。
- AI 图片生成失败不能影响文字剪辑和视频导出。

### 5.7 F-07 滤镜与动态效果

MVP 只提供三档效果：

- 关闭：不增加任何自动效果。
- 自然：轻微提升对比度和饱和度，在部分长镜头中缓慢推近。
- 活力：更明显的色彩和少量关键词强调效果。

系统只在以下情况增加动态效果：

- 长时间固定画面时进行轻微推近。
- 明显跳切时使用极短淡化或让艺术字/画中画覆盖切点。
- 章节变化时使用一次简单淡入淡出。

用户可以全局切换效果档位，不需要逐项配置复杂参数。

### 5.8 F-08 预览、保存与导出

- 浏览器使用 720p 代理视频预览。
- 预览包含文字删减、艺术字、画中画和基础效果的大致结果。
- 页面刷新后可恢复最近保存的工程。
- 导出格式固定为 MP4/H.264，默认 1080p，帧率跟随原视频。
- 导出任务显示“排队、处理中、完成、失败”状态。
- 页面关闭后，已经开始的导出任务可以继续执行。
- 导出完成后提供下载按钮。

## 6. 页面设计

### 6.1 首页/上传页

- 新建工程按钮。
- 视频上传区域。
- 最近工程列表。

### 6.2 处理页

- 上传、生成预览、语音识别三个阶段的进度。
- 失败原因和重试按钮。

### 6.3 编辑页

- 左侧：转写文字和删减建议。
- 中间：视频预览。
- 右侧：艺术字、AI 图片和效果设置。
- 底部：播放、当前时间、撤销、重做和导出按钮。

MVP 不展示复杂多轨时间线。

## 7. 验收标准

### 7.1 文字剪辑

- 上传一段 5–10 分钟普通话口播后，可以得到带时间信息的文字。
- 点击文字可以跳转视频，播放时当前句正确高亮。
- 删除一句文字后，预览和导出均不包含对应视频内容。
- 修改错别字不会误删视频。
- 删除操作可以撤销和恢复。
- 常见切点没有明显吞字或爆音。

### 7.2 艺术字

- 系统能识别测试文案中的标题、关键词和数字。
- 用户可以在三种模板中选择并修改文字、颜色和位置。
- 艺术字在预览和导出中的时间基本一致。

### 7.3 AI 图片

- 系统能给出 1–3 个与内容有关的图片建议。
- 用户确认后可以生成、重新生成并插入图片。
- 图片生成失败时仍能正常保存和导出主视频。

### 7.4 滤镜与效果

- 关闭、自然、活力三档有明确差异。
- 关闭后导出视频不包含自动效果。
- 默认效果不会频繁闪烁或大幅缩放。

### 7.5 工程与导出

- 刷新页面后工程内容不会丢失。
- 10 分钟 1080p 测试视频可以成功导出 MP4。
- 导出后的音画同步误差不超过 2 帧。
- 导出失败时显示原因并允许重试。

## 8. MVP 质量要求

- Chrome、Edge 桌面版可正常使用。
- 常用编辑操作点击后 300ms 内有界面反馈。
- 后台处理不能导致网页失去响应。
- 原视频不会被编辑操作覆盖。
- API 密钥不能出现在浏览器代码或日志中。
- 用户不能通过修改工程地址访问其他用户的视频。
- 用户删除工程后，系统可以删除对应文件。

## 9. 成功指标

MVP 测试阶段建议只观察以下指标：

- 语音转写成功率。
- 用户完成首次导出的比例。
- AI 删减建议接受率。
- 艺术字和 AI 图片的保留率。
- 完成一段 10 分钟口播粗剪所需时间。
- 导出成功率和主要失败原因。

## 10. 开发排期建议

| 阶段 | 时间 | 交付结果 |
|---|---:|---|
| 第一阶段 | 第 1 周 | 上传、预览视频、语音转写 |
| 第二阶段 | 第 2 周 | 文字删除、预览跳切、基础导出 |
| 第三阶段 | 第 3 周 | AI 删减建议、十一种艺术字模板 |
| 第四阶段 | 第 4 周 | AI 图片画中画、三档滤镜效果 |
| 测试优化 | 第 5–6 周 | 保存恢复、错误处理、体验和导出稳定性 |

## 11. 开发前只需确认的问题

1. MVP 是内部演示，还是需要开放给少量真实用户？
2. 单个视频最大时长和大小是否接受 30 分钟、1GB？
3. 文本模型和图片生成服务是否已有指定供应商？语音识别已确定使用阿里云百炼 Paraformer。
4. 艺术字是否已有设计参考，还是使用现有十一套默认样式？
5. 首次上线使用国内服务器还是海外服务器？
